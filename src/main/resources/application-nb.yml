spring:
  profiles: nb
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/scanner
    username: postgres
    password: 12345

server:
  error:
    whitelabel:
      enabled: false
